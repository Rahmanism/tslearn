// Anonymous Types
var people;
people = {
    name: "Mostafa"
};
console.log(people);
function totalLength(x, y) {
    return x.length + y.length;
}
var x = { length: 120 };
console.log(totalLength(x, x));
